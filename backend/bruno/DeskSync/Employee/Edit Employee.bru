meta {
  name: Edit Employee
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/api/employee/9
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "firstname": "test"
  }
}
